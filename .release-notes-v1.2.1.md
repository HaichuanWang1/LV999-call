## 语音识别大幅改善（本版重点）

上一版之后集中修了一轮「识别不准」。根因不只是模型本身，更多在**送进 ASR 之前的录音环节**。

### 录音前端

- **只上传真正有语音的片段**。此前把「按下录音 → 检测到停顿」之间的全部原始数据整段送走，
  其中包含开头最多 3 秒静音 —— 而喂纯静音正是 Whisper 类模型最经典的幻觉触发器，
  它会凭空输出一段内容。现在只裁出 `[语音起点-300ms, 终点+200ms]`
- **修「第一个字经常被吃掉」**。VAD 需要连续若干帧超阈值才认定「开始说话」，
  那几帧之前的声音原本被直接丢掉；现在保留 300ms 预滚把起音补回来
- **音源改用 `VOICE_RECOGNITION`**（该路径下系统必须关闭 AGC 与降噪），失败回退 `MIC`，
  并显式关闭 AGC/降噪/回声消除。AGC 会在句间停顿把底噪顶上来，让固定阈值的 VAD 持续误触发
- **时间常量全部改成毫秒**。旧版按「帧数」写死，而帧长随机型在 40~128ms 浮动 ——
  同一份代码在不同手机上「说完等 1.8 秒」还是「等 5.8 秒」完全看运气
- **噪声底按环境自适应**（校准期用能量均值 + 慢速跟踪），替代写死的 `0.02`：
  说话偏轻的人不再触发不了，嘈杂环境不再一直误触发；配进入/退出双阈值迟滞，
  避免一句话在阈值附近被切碎
- 裁剪按**真实帧字节偏移**换算，而非「帧号 × 固定帧长」——后者在 `read()`
  返回短帧时会整体错位

### Vosk 离线：修一个会整段丢字的 bug

旧实现全程只调 `acceptWaveForm`、最后才取一次 `finalResult`。而 Vosk 一旦在音频中间
检测到端点（一段话结束的停顿），就把状态置为 `ENDPOINT`，此后 `finalResult`
**只会返回空串** —— 也就是说「说话中间有停顿」就整段丢字。改为边送边收 `result()` 拼接，
与官方示例一致。另外中文模型词表是单字，输出形如「你 好 世 界」，
现在会去掉汉字之间的空格再交给 LLM。

### HTTP ASR

- 新增 **ASR 模型名**设置项并真正下发。OpenAI 的 `/v1/audio/transcriptions`
  把 `model` 列为必填，缺失直接 400 —— 而旧代码声明了这个参数却从未传值，
  导致官方端点必然失败。自建服务端可留空
- 语言码归一化为 ISO-639-1（`zh-CN` → `zh`，填 `auto` 则不发该字段）
- 失败自动重试一次，仍失败且离线模型已加载时回退 Vosk

### 识别失败不再污染聊天记录

识别为空时，旧版会**伪造一条 `（语音识别失败）` 的用户消息**并持久化进历史 ——
失败一次，记录里就永久多一条假发言，还会被当作真实输入送进 LLM 上下文。
现在改为状态条短暂提示「没听清，再说一次～」并自动回到聆听。

## 新增内置预设：DeepSeek 酱 🐳

与银狼**并列**的新角色（傲娇干饭鲸鱼娘），不是替换关系。

- 使用 DS鲸鱼娘 Live2D 模型（作者：B 站 UP 主 **氵六青**），带待机与吹泡泡、自拍等动作，
  并绑定 LLM 可调度的表情集
- 发声**强制锁定** MiMo 预置少女音「冰糖」，无视设置里选的 TTS 模型；
  配了专属语气提示词（清亮软糯、语速稍快、带点懒洋洋的傲娇感）
- 该模型没有「变身」这类一次性演出，因此关闭了接通/挂断过场 ——
  硬播会在接通瞬间定格一个怪动作

### 顺带把「预设」改成了可并列的结构

原先提示词、表情集、发声策略都是全局单例，加入第二个角色后无法共存
（在 DeepSeek酱 页面改一句语气会连带把银狼的也改掉）。现在全部下移到
`BuiltInCharacter` 字段驱动，调用链里不再出现任何「如果是银狼就……」的分支。

## 修复

- **TTS 全程「哒哒」杂音**。流式请求 `format=wav` 时，服务端把**每个 SSE 分块**
  都返回成一个独立完整 WAV，各带 44 字节 RIFF 头；播放端只认得开头那一个头，
  后续每块的头都被当成 PCM 采样写进 AudioTrack，每块边界爆出 22 个垃圾采样
  （听感约每秒 3 下）。改用 `pcm16` 并就地剥头兜底
- **DeepSeek酱 显示成银狼**：角色形象参数异步就绪时会重载模型
- **自定义预设丢失表情驱动**：并列化改造中回归，已恢复
- 自定义预设保持原有的变身过场行为

## 其他

- 设置页移除「TTS播完再录音」开关 —— 它从未被任何代码读取过。
  实际行为一直是「TTS 播完再开始录音」，与开关状态无关
- 删除 `ChatRepository.transcribeAudio`（`AsrEngine` 的重复实现，且未做 PCM→WAV 转换，无调用方）

## 已知问题 / 待真机确认

- 离线小模型（`vosk-model-small-cn-0.22`，约 42MB）官方 CER 为 23.54% / 38.29%，
  这是离线方案的天花板。追求更高准确率建议改用「自定义 HTTP」接云端 ASR
