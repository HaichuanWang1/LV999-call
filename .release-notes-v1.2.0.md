## 通话界面重构（本版重点）

把 Live2D 模型与对话框装进**同一个弱边界板块**，并重做气泡的出场与生成过程。

### 气泡：从"突然出现"到有过程

- **思考中转圈**：说完话的瞬间就挂上气泡内的三点脉冲占位，不再是一段空白后
  文字直接蹦出来。ASR 那段等待也有反馈
- **无缝衔接**：占位与流式文本共用**同一个气泡、同一个 item key**，
  首字到达时是原地换内容（`Crossfade`），不是"旧气泡消失 + 新气泡出现"
- **入场动画**：每条气泡淡入 + 轻微放大 + 上滑。用 `graphicsLayer` 实现
  （只影响绘制、不参与布局），避免 `AnimatedVisibility` 让 item 高度从 0 长出来
  把滚动位置顶跳；配记账机制，列表回收重建时不重播
- **灰色半透明**：气泡改为中性灰 + 1dp 弱描边，深浅主题各一套，角色能从气泡后透出来

### 板块化

- Live2D 舞台与对话区装在一个容器里，内部只用一条分区线分隔 ——
  一个整块天然没有两块卡片之间的缝隙、错位与互相遮挡
- 模型改为**按舞台区实际宽高比动态适配**（`setLayout` 新增 `fitBy`），
  四周留 6% 安全边距，不再像全屏铺底时那样把角色头脚裁掉
- 装 WebView 的容器**刻意不做圆角裁剪**：`AndroidView` 是真实 View，
  Compose 裁剪对它的硬件层处理不一致，实测会导致模型整块不上屏。
  "不越界"改由布局保证

## 修复

- **通话记录气泡成对重复**（存在已久的 bug）。根因：`MessageEntity` 主键是自增 id，
  `OnConflictStrategy.REPLACE` 永远命不中冲突，而每轮结束都会把「当前完整消息列表」
  整体写库 —— 每轮把已有消息再追加一遍，第 2 轮结束库里就成了 `u1 a1 u1 a1 u2 a2`。
  写入侧改为「替换」语义，读取侧对已有脏数据做自愈（结构性精确匹配，只读不写回）
- **思考内容外泄**：原先用闭合标签正则剥离 `<think>`，流式中间态里思考块还开着、
  正则匹配不到，整段推理会实时滚在气泡里；模型被截断时还会连着半截标签进 TTS 被念出来。
  改为逐块增量状态机：推理块内全部丢弃，只扣住可能没收全的标签前缀，
  流在块中途结束时余下内容一律丢弃

## LLM 参数

设置页新增三根滑杆（各带说明与「默认」复位）：

| 参数 | 默认 | 范围 |
|------|------|------|
| 温度 Temperature | 0.70 | 0 ~ 2 |
| 核采样 Top P | 1.00 | 0.1 ~ 1 |
| 最大输出长度 | 2048 | 128 ~ 4096 |

只放 OpenAI 兼容接口普遍支持的字段。`thinking` 这类各家私有的开关**不进设置页**，
且改为仅在确认支持的服务商上发送 —— 未知字段会让严格校验请求体的接口直接 400。
上下文裁剪时预留的 token 也改取配置值，不再写死 2048。

## 署名

Live2D 形象（银狼模型）的作者是 B 站 UP 主 **槿絮OuO**。已在通话页舞台区左下角
与设置页 Live2D 开关下方标注，点击可跳转到作者主页。

## 已知问题 / 待真机确认

- 模型在舞台区里的**取景比例**（大小、上下位置）是离线推算的，需要在真机上确认；
  参数集中在 `CallScreen.kt` 顶部：`STAGE_FILL_RATIO`（默认 0.94）、`offsetY`（默认 0.02）
- 舞台区与对话区的高度配比为 `1 : 1.2`，小屏设备上可能需要再调
- 历史页的自愈去重只在读取时生效、不写回：旧会话需「继续对话」并完成一轮后
  才会被覆盖干净
